To execute, simply run 188_FINAL_PROJECT.py. 

Outputs (important variable lists, function returns, plots, etc) are saved in the folders output/output_data and output/plots, while CMS data is stored in the data folder.

See the requirements document for all necessary packages
